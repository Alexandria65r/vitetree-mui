import { styled,Box, Typography } from '@mui/material'
import React from 'react'

const Container = styled(Box)(() => ({
    
}))


type Props = {}

export default function SubmitedTaskFiles({}: Props) {
  return (
      <Container>
          <Typography>Video Tutorial link</Typography>
      </Container>
  )
}