import React from 'react'
import Layout from '../components/layout'

type Props = {}

export default function Earnings({ }: Props) {
    return (
        <Layout>
            <div>Earnings</div>
        </Layout>
    )
}