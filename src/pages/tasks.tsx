import React from 'react'
import Layout from '../components/layout'

type Props = {}

export default function Tasks({ }: Props) {
    return (
        <Layout>
            <div>Tasks</div>
        </Layout>
    )
}