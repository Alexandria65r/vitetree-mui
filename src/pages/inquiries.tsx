import React from 'react'
import Layout from '../components/layout'

type Props = {}

export default function Inquiries({ }: Props) {
    return (
        <Layout>
            <div>Inquiries</div>
        </Layout>
    )
}