import React from 'react'
import Layout from '../../components/layout'

type Props = {}

export default function Meet({ }: Props) {
    return (
        <Layout>
            <div>Meet</div>
        </Layout>
    )
}